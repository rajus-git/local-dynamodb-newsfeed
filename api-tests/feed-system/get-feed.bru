meta {
  name: Get Feed (User B)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/feed/{{userB}}?limit=5
  body: none
  auth: none
}

params:query {
  limit: 5
}

tests {
  test("Feed fetched", function () {
    expect(res.getStatus()).to.equal(200)
  
    const body = res.getBody()
    if (body.nextToken) {
      bru.setEnvVar("nextToken", body.nextToken)
    }
  })
  
   test("Feed contains items", function () {
      const body = res.getBody()
      expect(body.items.length).to.be.greaterThan(0)
    })
}
