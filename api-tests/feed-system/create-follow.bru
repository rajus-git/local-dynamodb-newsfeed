meta {
  name: Create Follow (B follows A)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/follows
  body: json
  auth: none
}

body:json {
  {
    "userFollowing": "{{userB}}",
    "userFollowed": "{{userA}}"
  }
}

tests {
  test("Follow created", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
